---
phase: 01-foundation
plan: 06
type: execute
wave: 6
depends_on:
  - "04"
files_modified:
  - app/api/products/route.ts
autonomous: true
requirements:
  - PROD-01
user_setup: []
must_haves:
  truths:
    - Superadmin can create products with name, SKU, category
    - All users can view product catalog
    - SKU uniqueness enforced
---

<objective>
Implement product catalog endpoints. All users can view products; only superadmin can create.

Purpose: Foundation for inventory and POS in Phase 2
Output: POST and GET /api/products endpoints
</objective>

<execution_context>
@/Users/miftahramadhan/Projects/webstockname/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<tasks>

<task type="auto">
  <name>Implement POST /api/products (Create Product)</name>
  <files>app/api/products/route.ts</files>
  <action>
    Create app/api/products/route.ts POST handler:
    - Accept { name, sku, category }
    - Validate: name required 1-255 chars, sku unique 3-50 chars, category optional
    - Insert product
    - Return created product with 201
    - Require: authMiddleware + rbacMiddleware(["SUPERADMIN"])
  </action>
  <verify>
    - POST /api/products creates product
    - SKU uniqueness enforced (duplicate returns 400)
  </verify>
  <acceptance_criteria>
    - [ ] POST /api/products creates product
    - [ ] SKU uniqueness enforced
  </acceptance_criteria>
  <done>Create product endpoint implemented</done>
</task>

<task type="auto">
  <name>Implement GET /api/products (List Products)</name>
  <files>app/api/products/route.ts</files>
  <action>
    Add GET handler:
    - Return all products
    - Optional filters: ?category={category}, ?sku={sku}
    - No role restriction (all roles can view)
    - Require: authMiddleware only
  </action>
  <verify>
    - GET /api/products returns all products
    - Works for all roles
  </verify>
  <acceptance_criteria>
    - [ ] GET /api/products returns products
    - [ ] All roles can access
  </acceptance_criteria>
  <done>List products endpoint implemented</done>
</task>

<task type="auto">
  <name>Test Product CRUD</name>
  <files>app/api/products/</files>
  <action>
    Test:
    1. [ ] POST product as SUPERADMIN (201)
    2. [ ] POST product as CASHIER (403)
    3. [ ] GET products as all roles (200)
    4. [ ] Duplicate SKU rejected (400)
  </action>
  <verify>Manual testing</verify>
  <acceptance_criteria>
    - [ ] CRUD works as expected
  </acceptance_criteria>
  <done>Product endpoints tested</done>
</task>

</tasks>

---

## Definition of Done

- [x] POST /api/products implemented (SUPERADMIN only)
- [x] GET /api/products implemented (all roles)
- [x] SKU uniqueness enforced
- [x] Endpoints tested

---

*Plan created: 2026-04-14*
