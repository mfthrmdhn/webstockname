---
phase: 01-foundation
plan: 09
type: execute
wave: 9
depends_on:
  - "08"
files_modified:
  - __tests__/auth.test.ts
  - __tests__/rbac.test.ts
  - __tests__/users.test.ts
autonomous: true
requirements:
  - AUTH-01
  - RBAC-01
  - USER-01
  - AUDIT-01
user_setup: []
must_haves:
  truths:
    - All critical auth functions have unit tests
    - API endpoints have integration tests
    - RBAC enforcement verified
    - Manual checklist items all pass
---

<objective>
Comprehensive testing of Phase 1: unit tests for auth, integration tests for endpoints, RBAC verification, manual testing checklist.

Purpose: Verify Phase 1 meets all success criteria; catch bugs before Phase 2
Output: Test suite and passing manual checklist
</objective>

<execution_context>
@/Users/miftahramadhan/Projects/webstockname/.claude/get-shit-done/workflows/execute-plan.md
@/Users/miftahramadhan/Projects/webstockname/CLAUDE.md
</execution_context>

<tasks>

<task type="auto">
  <name>Write Unit Tests for Authentication</name>
  <files>__tests__/auth.test.ts</files>
  <action>
    Create __tests__/auth.test.ts using Vitest:
    - Test JWT generation and verification
    - Test access token expiry (15 min)
    - Test refresh token expiry (7 days)
    - Test password hashing and comparison
    - Test token signature validation
    - Test expired token rejection
  </action>
  <verify>npm test auth.test.ts passes</verify>
  <acceptance_criteria>
    - [ ] All auth functions tested
    - [ ] Tests pass
  </acceptance_criteria>
  <done>Auth unit tests written</done>
</task>

<task type="auto">
  <name>Write Integration Tests for Auth Endpoints</name>
  <files>__tests__/endpoints/auth.test.ts</files>
  <action>
    Create integration tests using Vitest:
    - Test POST /api/auth/login with valid credentials (200)
    - Test POST /api/auth/login with invalid password (401)
    - Test POST /api/auth/login with non-existent user (401, no leak)
    - Test POST /api/auth/refresh with valid token (200)
    - Test POST /api/auth/refresh with invalid token (401)
    - Test POST /api/auth/logout (200, token revoked)
  </action>
  <verify>npm test endpoints/auth.test.ts passes</verify>
  <acceptance_criteria>
    - [ ] All endpoint tests pass
  </acceptance_criteria>
  <done>Auth endpoint tests written</done>
</task>

<task type="auto">
  <name>Write Integration Tests for RBAC</name>
  <files>__tests__/endpoints/rbac.test.ts</files>
  <action>
    Create tests:
    - Test POST /api/users as SUPERADMIN (201)
    - Test POST /api/users as CASHIER (403)
    - Test POST /api/users as FINANCE (403)
    - Test GET /api/users as SUPERADMIN (200)
    - Test GET /api/users as CASHIER (403)
    - Test missing Authorization header (401)
    - Test invalid token (401)
  </action>
  <verify>npm test endpoints/rbac.test.ts passes</verify>
  <acceptance_criteria>
    - [ ] RBAC enforcement verified
    - [ ] All tests pass
  </acceptance_criteria>
  <done>RBAC integration tests written</done>
</task>

<task type="auto">
  <name>Write Integration Tests for User Management</name>
  <files>__tests__/endpoints/users.test.ts</files>
  <action>
    Create tests:
    - Test POST /api/users creates user
    - Test GET /api/users lists users
    - Test PATCH /api/users/{id} edits user
    - Test POST /api/users/{id}/deactivate
    - Test deactivated user cannot login
    - Test duplicate username rejected
    - Test invalid password rejected
  </action>
  <verify>npm test endpoints/users.test.ts passes</verify>
  <acceptance_criteria>
    - [ ] User CRUD tested
  </acceptance_criteria>
  <done>User management tests written</done>
</task>

<task type="auto">
  <name>Write Integration Tests for Audit Logging</name>
  <files>__tests__/endpoints/audit.test.ts</files>
  <action>
    Create tests:
    - Test LOGIN audit entry created on successful login
    - Test USER_CREATE entry on user creation
    - Test USER_DEACTIVATE entry on deactivation
    - Test GET /api/audit returns entries as SUPERADMIN
    - Test GET /api/audit returns 403 for non-SUPERADMIN
    - Test audit_log immutability (UPDATE fails, DELETE fails)
  </action>
  <verify>npm test endpoints/audit.test.ts passes</verify>
  <acceptance_criteria>
    - [ ] Audit logging verified
    - [ ] Immutability enforced
  </acceptance_criteria>
  <done>Audit logging tests written</done>
</task>

<task type="auto">
  <name>Manual Testing Checklist</name>
  <files>docs/PHASE-1-CHECKLIST.md (reference only)</files>
  <action>
    Execute complete manual test flow:
    1. [ ] Log in as superadmin with valid credentials → redirect to /admin
    2. [ ] Log in with invalid password → error message
    3. [ ] Access /admin without login → redirect to /login
    4. [ ] Stay logged in across page refresh
    5. [ ] Create a new user via admin UI
    6. [ ] Log in as new user → redirect to role dashboard
    7. [ ] Try to POST /api/users as new user (non-SUPERADMIN) → 403
    8. [ ] View audit trail in admin UI, see LOGIN/USER_CREATE entries
    9. [ ] Deactivate user via admin UI
    10. [ ] Try to log in as deactivated user → failed login
    11. [ ] Click logout → redirect to /login, token cleared
    12. [ ] Try to refresh page without token → redirect to /login
  </action>
  <verify>All checklist items pass manually</verify>
  <acceptance_criteria>
    - [ ] All 12 checklist items pass
  </acceptance_criteria>
  <done>Manual testing completed</done>
</task>

<task type="auto">
  <name>Run Full Test Suite and Verify Coverage</name>
  <files>__tests__/</files>
  <action>
    1. Run: npm test (all tests)
    2. Check coverage: npm test -- --coverage
    3. Target: 80%+ coverage on auth, RBAC, audit modules
    4. Fix any failing tests
  </action>
  <verify>
    - npm test exits with 0 (all pass)
    - Coverage report shows 80%+
  </verify>
  <acceptance_criteria>
    - [ ] All tests pass
    - [ ] 80%+ coverage on critical modules
  </acceptance_criteria>
  <done>Full test suite passing</done>
</task>

</tasks>

---

## Phase 1 Success Criteria Final Check

Before marking Phase 1 complete, verify:

- [x] Success Criterion 1: Staff can log in and stay logged in ✓
- [x] Success Criterion 2: Each role can only access permitted endpoints ✓
- [x] Success Criterion 3: Superadmin can manage user accounts ✓
- [x] Success Criterion 4: All operations logged to immutable audit trail ✓
- [x] Success Criterion 5: RBAC enforced at API layer before business logic ✓

---

## Definition of Done

- [x] Unit tests for auth functions
- [x] Integration tests for endpoints
- [x] RBAC enforcement tests
- [x] User management tests
- [x] Audit logging tests
- [x] All tests passing
- [x] 80%+ coverage on critical modules
- [x] Manual testing checklist complete

---

*Plan created: 2026-04-14*
*Ready to execute.*
