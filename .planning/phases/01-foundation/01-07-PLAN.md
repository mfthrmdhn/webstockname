---
phase: 01-foundation
plan: 07
type: execute
wave: 7
depends_on:
  - "06"
files_modified:
  - app/admin/page.tsx
  - app/admin/users/page.tsx
  - app/admin/products/page.tsx
  - app/admin/audit/page.tsx
  - components/AdminNav.tsx
autonomous: true
requirements: []
user_setup: []
must_haves:
  truths:
    - Superadmin dashboard loads at /admin
    - User management page shows user table
    - Product management page shows product table
    - Audit log page shows audit entries
    - Only SUPERADMIN role can access /admin
---

<objective>
Build Superadmin dashboard UI with user, product, and audit management interfaces.

Purpose: Enable staff account and product management
Output: Protected /admin dashboard with 4 pages
</objective>

<execution_context>
@/Users/miftahramadhan/Projects/webstockname/.claude/get-shit-done/workflows/execute-plan.md
@/Users/miftahramadhan/Projects/webstockname/CLAUDE.md
</execution_context>

<tasks>

<task type="auto">
  <name>Create Admin Layout with Navigation</name>
  <files>app/admin/layout.tsx, components/AdminNav.tsx</files>
  <action>
    1. Create app/admin/layout.tsx (protected, SUPERADMIN only)
    2. Create components/AdminNav.tsx with sidebar navigation:
       - Users
       - Products
       - Audit Log
       - Logout
    3. Add middleware check: redirect non-SUPERADMIN to /login
  </action>
  <verify>
    - /admin loads for SUPERADMIN
    - Non-SUPERADMIN redirected to /login
  </verify>
  <acceptance_criteria>
    - [ ] Admin layout protected
    - [ ] Navigation sidebar present
  </acceptance_criteria>
  <done>Admin layout created</done>
</task>

<task type="auto">
  <name>Create User Management UI</name>
  <files>app/admin/users/page.tsx</files>
  <action>
    1. Create page with user table (username, role, is_active)
    2. "Create User" button → modal with form (username, password, role)
    3. Click row → edit modal (username, role, submit)
    4. "Deactivate" button per user (confirm modal)
    5. Form validation matches backend
    6. API calls: POST/GET/PATCH /api/users
    7. Toast notifications for success/error
  </action>
  <verify>
    - Create user form works
    - Edit user works
    - Deactivate works
  </verify>
  <acceptance_criteria>
    - [ ] User table displays
    - [ ] CRUD operations work
    - [ ] Form validation present
  </acceptance_criteria>
  <done>User management UI created</done>
</task>

<task type="auto">
  <name>Create Product Management UI</name>
  <files>app/admin/products/page.tsx</files>
  <action>
    1. Create page with product table (name, sku, category)
    2. "Create Product" button → modal with form (name, sku, category)
    3. Form validation matches backend
    4. API calls: POST/GET /api/products
    5. Toast notifications
  </action>
  <verify>
    - Product table displays
    - Create product works
  </verify>
  <acceptance_criteria>
    - [ ] Product table displays
    - [ ] Create product works
  </acceptance_criteria>
  <done>Product management UI created</done>
</task>

<task type="auto">
  <name>Create Audit Log Viewing UI</name>
  <files>app/admin/audit/page.tsx</files>
  <action>
    1. Create page with audit entries table (user, action, entity, timestamp)
    2. Filters: date range (from/to), action dropdown, user dropdown
    3. Pagination (50 per page)
    4. Readable timestamps (e.g., "2026-04-14 10:30 AM")
    5. API call: GET /api/audit with filters
  </action>
  <verify>
    - Audit table displays
    - Filters work
  </verify>
  <acceptance_criteria>
    - [ ] Audit entries display
    - [ ] Filtering works
  </acceptance_criteria>
  <done>Audit log UI created</done>
</task>

<task type="auto">
  <name>Test Admin Dashboard End-to-End</name>
  <files>app/admin/</files>
  <action>
    Test:
    1. [ ] /admin loads for SUPERADMIN
    2. [ ] /admin redirects to /login for non-SUPERADMIN
    3. [ ] Can create, edit, deactivate users
    4. [ ] Can create products
    5. [ ] Can view and filter audit log
    6. [ ] Logout works
  </action>
  <verify>Manual testing in browser</verify>
  <acceptance_criteria>
    - [ ] All admin features work
  </acceptance_criteria>
  <done>Admin dashboard tested</done>
</task>

</tasks>

---

## Definition of Done

- [x] Admin layout created and protected
- [x] User management UI implemented
- [x] Product management UI implemented
- [x] Audit log viewing UI implemented
- [x] Navigation sidebar present
- [x] All CRUD operations functional

---

*Plan created: 2026-04-14*
