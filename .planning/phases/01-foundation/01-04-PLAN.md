---
phase: 01-foundation
plan: 04
type: execute
wave: 4
depends_on:
  - "03"
files_modified:
  - app/api/users/route.ts
  - app/api/users/[id]/route.ts
  - app/api/users/[id]/deactivate/route.ts
  - app/api/users/[id]/reset-password/route.ts
autonomous: true
requirements:
  - USER-01
  - USER-02
  - USER-03
  - USER-04
  - USER-05
user_setup: []
must_haves:
  truths:
    - Superadmin can create users with username, password, role
    - Can list all users
    - Can edit username and role
    - Can deactivate users (set is_active=false)
    - Can reset user passwords
    - All operations logged to audit_log
---

<objective>
Implement user CRUD endpoints for superadmin management. Only SUPERADMIN role can manage users.

Purpose: Enable staff account lifecycle management
Output: Four user management endpoints (POST, GET, PATCH, deactivate, reset-password)
</objective>

<execution_context>
@/Users/miftahramadhan/Projects/webstockname/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/01-foundation/01-03-PLAN.md (RBAC middleware)
</context>

<tasks>

<task type="auto">
  <name>Implement POST /api/users (Create User)</name>
  <files>app/api/users/route.ts</files>
  <read_first>User validation rules: username 3-50 chars, password 12+ chars, role must exist</read_first>
  <action>
    Create app/api/users/route.ts with POST handler:
    - Accept { username, password, role } 
    - Validate username unique, 3-50 chars
    - Validate password 12+ chars, 1 uppercase, 1 number
    - Validate role exists in database
    - Hash password with hashPassword()
    - Insert user record
    - Log USER_CREATE to audit_log
    - Return created user (exclude password_hash) with 201
    - Require: authMiddleware + rbacMiddleware(["SUPERADMIN"])
  </action>
  <verify>
    - POST /api/users with valid data returns 201
    - Invalid password returns 400
    - Duplicate username returns 400
  </verify>
  <acceptance_criteria>
    - [ ] POST /api/users creates user successfully
    - [ ] Password validation enforced
    - [ ] Username uniqueness enforced
    - [ ] USER_CREATE audit entry created
  </acceptance_criteria>
  <done>Create user endpoint implemented</done>
</task>

<task type="auto">
  <name>Implement GET /api/users (List Users)</name>
  <files>app/api/users/route.ts</files>
  <action>
    Add GET handler to app/api/users/route.ts:
    - Query all users with their role names
    - Exclude password_hash from response
    - Optional filters: ?is_active=true, ?role={role_id}
    - Return 200 with users array
    - Require: authMiddleware + rbacMiddleware(["SUPERADMIN"])
  </action>
  <verify>grep -q "GET.*users" app/api/users/route.ts</verify>
  <acceptance_criteria>
    - [ ] GET /api/users returns all users
    - [ ] password_hash not included in response
    - [ ] Filters work correctly
  </acceptance_criteria>
  <done>List users endpoint implemented</done>
</task>

<task type="auto">
  <name>Implement PATCH /api/users/{id} (Edit User)</name>
  <files>app/api/users/[id]/route.ts</files>
  <action>
    Create app/api/users/[id]/route.ts with PATCH handler:
    - Accept { username, role } (no password editing here)
    - Validate username uniqueness (across all users except self)
    - Validate role exists
    - Update user record
    - Log USER_EDIT to audit_log
    - Return updated user with 200
    - Require: authMiddleware + rbacMiddleware(["SUPERADMIN"])
  </action>
  <verify>
    - PATCH /api/users/{id} updates user successfully
    - Returns 404 for non-existent user
  </verify>
  <acceptance_criteria>
    - [ ] PATCH updates username and role
    - [ ] USER_EDIT audit entry created
    - [ ] Returns updated user (no password)
  </acceptance_criteria>
  <done>Edit user endpoint implemented</done>
</task>

<task type="auto">
  <name>Implement POST /api/users/{id}/deactivate (Deactivate User)</name>
  <files>app/api/users/[id]/deactivate/route.ts</files>
  <action>
    Create app/api/users/[id]/deactivate/route.ts:
    - Set is_active = false
    - Do NOT delete user record (preserve audit trail)
    - Log USER_DEACTIVATE to audit_log
    - Return 200
    - Require: authMiddleware + rbacMiddleware(["SUPERADMIN"])
    - Update login endpoint to check is_active before allowing login
  </action>
  <verify>
    - POST /api/users/{id}/deactivate sets is_active=false
    - Deactivated user cannot log in
  </verify>
  <acceptance_criteria>
    - [ ] POST /api/users/{id}/deactivate returns 200
    - [ ] USER_DEACTIVATE audit entry created
    - [ ] Deactivated user login rejected
  </acceptance_criteria>
  <done>Deactivate user endpoint implemented</done>
</task>

<task type="auto">
  <name>Implement POST /api/users/{id}/reset-password (Admin Password Reset)</name>
  <files>app/api/users/[id]/reset-password/route.ts</files>
  <action>
    Create app/api/users/[id]/reset-password/route.ts:
    - Accept { new_password }
    - Validate password (12+ chars, 1 uppercase, 1 number)
    - Hash with hashPassword()
    - Update user.password_hash
    - Log USER_EDIT to audit_log
    - Return 200
    - Require: authMiddleware + rbacMiddleware(["SUPERADMIN"])
  </action>
  <verify>grep -q "reset-password" app/api/users/</verify>
  <acceptance_criteria>
    - [ ] POST /api/users/{id}/reset-password hashes and stores password
    - [ ] Invalid password returns 400
    - [ ] USER_EDIT audit entry created
  </acceptance_criteria>
  <done>Password reset endpoint implemented</done>
</task>

<task type="auto">
  <name>Test User Management CRUD</name>
  <files>app/api/users/</files>
  <action>
    Test checklist:
    1. [ ] Create user as SUPERADMIN (200)
    2. [ ] Create user as CASHIER (403)
    3. [ ] List users as SUPERADMIN (200)
    4. [ ] List users as CASHIER (403)
    5. [ ] Edit user as SUPERADMIN (200)
    6. [ ] Deactivate user (is_active=false)
    7. [ ] Deactivated user cannot login
    8. [ ] Reset password for user
    9. [ ] Audit entries logged for all operations
  </action>
  <verify>Manual testing with curl or Postman</verify>
  <acceptance_criteria>
    - [ ] All CRUD operations work
    - [ ] RBAC enforced on all endpoints
    - [ ] Audit entries created
  </acceptance_criteria>
  <done>User management CRUD tested</done>
</task>

</tasks>

---

## Definition of Done

- [x] POST /api/users (create) implemented
- [x] GET /api/users (list) implemented
- [x] PATCH /api/users/{id} (edit) implemented
- [x] POST /api/users/{id}/deactivate implemented
- [x] POST /api/users/{id}/reset-password implemented
- [x] RBAC enforced on all endpoints
- [x] Audit logging on all operations
- [x] CRUD tested end-to-end

---

*Plan created: 2026-04-14*
