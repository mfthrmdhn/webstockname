---
phase: 02-operations
plan: 10
type: execute
wave: 9
gap_closure: true
depends_on: []
files_modified:
  - app/api/cashier/products/route.ts
  - app/cashier/pos/page.tsx
autonomous: true
requirements: [RBAC-03, PROD-02]

must_haves:
  truths:
    - "CASHIER role cannot see cost field via /api/cashier/products endpoint"
    - "cost field is removed from Product interface in POS component"
    - "API response includes only id, name, sku, price, category, store_qty, warehouse_qty"
    - "TypeScript compiler rejects any code that accesses product.cost in POS context"
  artifacts:
    - path: "app/api/cashier/products/route.ts"
      provides: "Product list endpoint (CASHIER restricted view)"
      critical_section: "select block in prisma.product.findMany (ensure cost: false or absent)"
    - path: "app/cashier/pos/page.tsx"
      provides: "POS interface (cashier UI without cost visibility)"
      critical_section: "Product type definition (no cost property)"
  key_links:
    - from: "app/api/cashier/products/route.ts (select block)"
      to: "response JSON"
      via: "select: { id, name, sku, price, category, store_qty, warehouse_qty } — cost NOT included"
      pattern: "cost:\\s*false|cost is removed"
    - from: "app/cashier/pos/page.tsx"
      to: "Product type usage"
      via: "type Product = { id, name, sku, price, category, store_qty, warehouse_qty }"
      pattern: "type Product|interface Product"
---

<objective>
Remove cost field exposure to CASHIER role in products endpoint and POS interface.

Purpose: Enforce RBAC boundary — only FINANCE role should see cost data for margin calculations. CASHIER role must not access cost at API level or in component state.
Output: CASHIER API response excludes cost; POS interface has no cost field in Product type.
</objective>

<execution_context>
@/Users/miftahramadhan/Projects/webstockname/.claude/get-shit-done/workflows/execute-plan.md
@/Users/miftahramadhan/Projects/webstockname/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/miftahramadhan/Projects/webstockname/.planning/PROJECT.md
@/Users/miftahramadhan/Projects/webstockname/.planning/ROADMAP.md
@/Users/miftahramadhan/Projects/webstockname/.planning/STATE.md
@/Users/miftahramadhan/Projects/webstockname/.planning/phases/02-operations/02-CONTEXT.md
@/Users/miftahramadhan/Projects/webstockname/.planning/phases/02-operations/02-REVIEW.md

<!-- CR-02 from code review: cost field exposed to CASHIER role -->
<!-- Current state: app/api/cashier/products/route.ts includes cost: true in select block -->
<!-- Current state: app/cashier/pos/page.tsx includes cost property in Product interface -->
<!-- Impact: RBAC violation — cashier can see product cost, enabling unauthorized discount logic -->
<!-- Fix: Remove cost: true from select block, remove cost property from Product type -->
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove cost field from /api/cashier/products endpoint</name>
  <read_first>
    - app/api/cashier/products/route.ts (full file — locate select block in prisma.product.findMany, understand current field list)
  </read_first>
  <action>
In `app/api/cashier/products/route.ts`, locate the `prisma.product.findMany` call and its select block. Ensure `cost: true` is either set to `false` or removed entirely.

**Current state (BROKEN):**
```typescript
const products = await prisma.product.findMany({
  where: { /* filters */ },
  select: {
    id: true,
    name: true,
    sku: true,
    price: true,
    cost: true,                // ← RBAC VIOLATION: CASHIER should not see cost
    category: true,
    store_qty: true,
    warehouse_qty: true,
  },
})
```

**Fixed state:**
```typescript
const products = await prisma.product.findMany({
  where: { /* filters */ },
  select: {
    id: true,
    name: true,
    sku: true,
    price: true,
    // cost: removed — only FINANCE role has /api/finance/products endpoint with cost
    category: true,
    store_qty: true,
    warehouse_qty: true,
  },
})
```

**Key points:**
- Remove or set `cost: false` — both work; removal is cleaner and more explicit
- All other fields (id, name, sku, price, category, store_qty, warehouse_qty) remain unchanged
- Response JSON will not include cost field in the array returned
- FINANCE role has a separate endpoint (`/api/finance/products`) that DOES include cost (not touched in this plan)
- SUPERADMIN has a separate endpoint (`/api/admin/products`) that DOES include cost (not touched in this plan)

**Why this fixes the gap:**
- Currently: CASHIER can call GET /api/cashier/products and see cost field, revealing profit margin (cost vs. price)
- After fix: CASHIER can only see public-facing fields (id, name, sku, price, category, inventory)
- Cost data remains available to FINANCE and SUPERADMIN via their respective endpoints
  </action>
  <acceptance_criteria>
- Grep confirms `cost: true` does not exist in the select block: `grep -A 12 "select: {" app/api/cashier/products/route.ts | grep -v "cost:" > /dev/null && echo pass || echo fail` (should find no `cost:` line)
- Grep confirms the remaining fields are still present: `grep "id: true" app/api/cashier/products/route.ts && grep "price: true" app/api/cashier/products/route.ts && grep "store_qty: true" app/api/cashier/products/route.ts`
- File compiles without errors: `npx tsc --noEmit app/api/cashier/products/route.ts`
- The select block structure is intact (only cost removed, no other fields modified)
  </acceptance_criteria>
  <verify>
    <automated>grep -n "cost:\s*true" /Users/miftahramadhan/Projects/webstockname/app/api/cashier/products/route.ts | wc -l | xargs -I {} bash -c 'if [ {} -eq 0 ]; then echo "✓ cost field removed from endpoint"; else echo "✗ cost field still present"; fi'</automated>
  </verify>
  <done>
- cost field removed from select block in /api/cashier/products
- Endpoint response no longer includes cost for CASHIER role
- FINANCE and SUPERADMIN endpoints continue to expose cost via their respective routes
- Field count in response: id, name, sku, price, category, store_qty, warehouse_qty (7 fields, no cost)
  </done>
</task>

<task type="auto">
  <name>Task 2: Remove cost property from Product type in POS component</name>
  <read_first>
    - app/cashier/pos/page.tsx (full file — locate Product type/interface definition, confirm cost is not used anywhere in the component)
  </read_first>
  <action>
In the Product type/interface used by `app/cashier/pos/page.tsx`, remove the `cost?: number` property (or `cost: number` if not optional).

**Current state (BROKEN):**
```typescript
type Product = {
  id: string
  name: string
  sku: string
  price: number
  category: string
  cost: number          // ← DEAD CODE: never read, never rendered, contradicts API response after Task 1
  store_qty: number
  warehouse_qty: number
}
```

**Fixed state:**
```typescript
type Product = {
  id: string
  name: string
  sku: string
  price: number
  category: string
  store_qty: number
  warehouse_qty: number
}
```

**Key points:**
- Remove the entire `cost: number` line (or `cost?: number` if optional)
- Do NOT add a comment like `// cost removed` — the removal itself is the change
- TypeScript will catch any code still trying to access `product.cost`
- After Task 1 completes, the API no longer returns cost, so removing it from the interface ensures the type matches the actual data
- If any code tries to reference `product.cost` (e.g., `const margin = price - product.cost`), TypeScript compilation will fail with: `Property 'cost' does not exist on type 'Product'`

**Verification:** Code review confirmed (IN-04) that cost is never used in the POS component:
- Not rendered in any JSX element
- Not stored in cart state
- Not used in any calculation
- Not passed to any child component
So removing it has zero functional impact and improves type safety.

**Why this fixes the gap:**
- Currently: Type says Product includes cost, which is misleading (API doesn't return it, component doesn't use it)
- After fix: Type accurately reflects API contract and component usage
- Prevents accidental future usage of cost data in CASHIER context (TypeScript enforces the boundary)
  </action>
  <acceptance_criteria>
- Grep confirms cost property is not in Product type: `grep -A 9 "type Product\|interface Product" app/cashier/pos/page.tsx | grep -v "cost" > /dev/null && echo pass || echo fail` (should find no `cost:` or `cost?:` in the type)
- File compiles without errors: `npx tsc --noEmit app/cashier/pos/page.tsx`
- No TypeScript errors about missing properties on Product type
- Other Product fields remain: id, name, sku, price, category, store_qty, warehouse_qty (7 fields, no cost)
  </acceptance_criteria>
  <verify>
    <automated>grep -n "cost" /Users/miftahramadhan/Projects/webstockname/app/cashier/pos/page.tsx | grep -E "^\s*(cost\s*[:?]|interface Product|type Product)" | wc -l | xargs -I {} bash -c 'if [ {} -eq 0 ]; then echo "✓ cost removed from interface"; else echo "✗ cost still in interface"; fi'</automated>
  </verify>
  <done>
- cost property removed from Product type in POS component
- TypeScript enforces that no CASHIER code can access product.cost
- Component compiles successfully with cost field absent
- POS interface now accurately reflects API contract (no cost field)
- Type safety improved: prevents accidental cost usage in future CASHIER code
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CASHIER API→Database | CASHIER /api endpoint must not expose cost data |
| CASHIER UI→API | POS component receives only CASHIER-safe fields (no cost) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-10-01 | Information Disclosure | Cost data visibility | mitigate | Remove cost from select block; only FINANCE and SUPERADMIN APIs expose cost |
| T-02-10-02 | Privilege Escalation | CASHIER→FINANCE data leakage | mitigate | CASHIER cannot derive margin (cost vs price) or access cost data; FINANCE has separate endpoint |
| T-02-10-03 | Tampering | Product response shape | mitigate | API response schema fixed; cost absence enforced at database query level |
</threat_model>

<verification>
1. Run `npm run dev` to start dev server
2. Log in as CASHIER user
3. Navigate to /cashier/pos
4. Open browser DevTools → Network tab → API calls
5. Search for a product (e.g., type "test" in search box)
6. Inspect the GET /api/cashier/products API response
7. Confirm response JSON does NOT include cost field:
   - Response should have: id, name, sku, price, category, store_qty, warehouse_qty
   - Response should NOT have: cost
8. Run TypeScript compiler: `npx tsc --noEmit` — confirm no errors
9. Try to build: `npm run build` — confirm no compilation errors about accessing product.cost
</verification>

<success_criteria>
- CASHIER API endpoint (/api/cashier/products) response excludes cost field entirely
- Product type in POS component has no cost property
- TypeScript compilation enforces that CASHIER code cannot access product.cost
- FINANCE role continues to have access to cost via separate /api/finance/products endpoint
- SUPERADMIN role continues to have access to cost via separate /api/admin/products endpoint
- No orphaned cost references or sneaky cost field aliases in CASHIER context
</success_criteria>

<output>
After completion, create `.planning/phases/02-operations/02-10-SUMMARY.md`
</output>
