---
phase: 02-operations
plan: 10
type: execute
wave: 9
gap_closure: true
depends_on: []
files_modified:
  - app/api/cashier/products/route.ts
  - app/cashier/pos/page.tsx
autonomous: true
requirements: [RBAC-03, PROD-02]

must_haves:
  truths:
    - "Cost field is not exposed in GET /api/cashier/products response"
    - "CASHIER role cannot see unit cost or margin data via API"
    - "Product interface in POS page does not include cost field"
    - "Dead code (cost in interface) is removed"
    - "SUPERADMIN and FINANCE can still view cost in their respective endpoints"
  artifacts:
    - path: "app/api/cashier/products/route.ts"
      provides: "Product search API for CASHIER role"
      select_block_must_not_have: "cost: true"
    - path: "app/cashier/pos/page.tsx"
      provides: "POS UI with Product interface"
      interface_must_not_have: "cost: number"
  key_links:
    - from: "app/api/cashier/products/route.ts"
      to: "response.json"
      via: "select block determines fields in response"
      pattern: "select: \\{.*\\}"
    - from: "app/cashier/pos/page.tsx"
      to: "fetch /api/cashier/products"
      via: "Product interface types the response"
      pattern: "interface Product"
---

<objective>
Remove sensitive cost (margin) data from cashier-facing endpoints and UI components.

Purpose: Enforce data privacy — cashiers should not see unit costs or profit margins; this is SUPERADMIN/FINANCE-only information.
Output: Cashier products endpoint excludes cost field; POS page interface reflects API contract.
</objective>

<execution_context>
@/Users/miftahramadhan/Projects/webstockname/.claude/get-shit-done/workflows/execute-plan.md
@/Users/miftahramadhan/Projects/webstockname/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/miftahramadhan/Projects/webstockname/.planning/PROJECT.md
@/Users/miftahramadhan/Projects/webstockname/.planning/ROADMAP.md
@/Users/miftahramadhan/Projects/webstockname/.planning/STATE.md
@/Users/miftahramadhan/Projects/webstockname/.planning/phases/02-operations/02-CONTEXT.md
@/Users/miftahramadhan/Projects/webstockname/.planning/phases/02-operations/02-REVIEW.md

<!-- CR-02 from code review: cost field exposed to CASHIER via products endpoint -->
<!-- Issue: app/api/cashier/products/route.ts line 33 includes cost: true in select -->
<!-- Issue: app/cashier/pos/page.tsx line 22 includes cost: number in Product interface (dead code) -->
<!-- Impact: Margin data visible to CASHIER role (should be SUPERADMIN/FINANCE only) -->
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove cost field from cashier products endpoint</name>
  <read_first>
    - app/api/cashier/products/route.ts (lines 28-39 — the select block)
    - app/cashier/pos/page.tsx (lines 18-26 — the Product interface)
  </read_first>
  <action>
Remove the `cost: true` line from the select block in `app/api/cashier/products/route.ts`.

**Current code (line 28-39):**
```typescript
select: {
  id: true,
  name: true,
  sku: true,
  sellingPrice: true,
  cost: true,           // <-- REMOVE THIS LINE
  storeQty: true,
  warehouseQty: true,
},
```

**Fixed code:**
```typescript
select: {
  id: true,
  name: true,
  sku: true,
  sellingPrice: true,
  storeQty: true,
  warehouseQty: true,
},
```

**Why:** The cost field is unit purchase price (COGS). Exposing it to the CASHIER role reveals product margins — sensitive business data. Only SUPERADMIN and FINANCE roles should see cost data in their respective endpoints (admin/finance dashboards use different endpoints with proper RBAC guards).

**Verification:** After removal, the endpoint will return products with:
- id, name, sku, sellingPrice, storeQty, warehouseQty
- NO cost field
  </action>
  <acceptance_criteria>
- Grep confirms cost is NOT in the select block: `grep -A 10 "select: {" app/api/cashier/products/route.ts | grep -c "cost" | grep -q "^0$"` (returns 0 matches)
- File compiles: `npx tsc --noEmit app/api/cashier/products/route.ts`
- Other select fields are unchanged (id, name, sku, sellingPrice, storeQty, warehouseQty all present)
  </acceptance_criteria>
  <verify>
    <automated>grep -A 8 "select: {" /Users/miftahramadhan/Projects/webstockname/app/api/cashier/products/route.ts | grep -q "cost:" && echo "✗ cost still in select" || echo "✓ cost removed from select"</automated>
  </verify>
  <done>
- cost field removed from the select block in GET /api/cashier/products
- Endpoint no longer exposes margin data to CASHIER role
- API response includes only: id, name, sku, sellingPrice, storeQty, warehouseQty
  </done>
</task>

<task type="auto">
  <name>Task 2: Remove cost from Product interface in POS page</name>
  <read_first>
    - app/cashier/pos/page.tsx (lines 18-26 — the Product interface)
  </read_first>
  <action>
Remove the `cost: number` field from the `Product` interface in `app/cashier/pos/page.tsx`.

**Current code (lines 18-26):**
```typescript
interface Product {
  id: string
  name: string
  sku: string
  sellingPrice: number
  cost: number          // <-- REMOVE THIS LINE
  storeQty: number
  warehouseQty: number
}
```

**Fixed code:**
```typescript
interface Product {
  id: string
  name: string
  sku: string
  sellingPrice: number
  storeQty: number
  warehouseQty: number
}
```

**Why:** The cost field is dead code — it's in the interface but never read, stored in the cart, or rendered anywhere in the component. Removing it aligns the TypeScript contract with what the API actually returns after Task 1. This also prevents accidental usage of cost data in future UI modifications.

**Verification:** After removal, all usages of the `Product` interface will still work correctly because cost is not used anywhere in the component (verified in code review IN-04).
  </action>
  <acceptance_criteria>
- Grep confirms cost is NOT in the Product interface: `grep -A 8 "interface Product {" app/cashier/pos/page.tsx | grep -c "cost" | grep -q "^0$"` (returns 0 matches)
- File compiles: `npx tsc --noEmit app/cashier/pos/page.tsx`
- No TypeScript errors in the component (cost was not being used, so removal does not break anything)
- All other fields in the interface are unchanged (id, name, sku, sellingPrice, storeQty, warehouseQty)
  </acceptance_criteria>
  <verify>
    <automated>grep -A 8 "interface Product {" /Users/miftahramadhan/Projects/webstockname/app/cashier/pos/page.tsx | grep -q "cost:" && echo "✗ cost still in interface" || echo "✓ cost removed from interface"</automated>
  </verify>
  <done>
- cost field removed from Product interface
- Interface now matches the API contract (no cost exposed to CASHIER)
- Dead code eliminated
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cashier←API | Cost data must not cross this boundary |
| cashier←UI | POS interface must not display or reference cost |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-10-01 | Information Disclosure | Cashier products API | mitigate | Remove cost from select block; RBAC['CASHIER','SUPERADMIN'] at endpoint entry but cost not exposed in response |
| T-02-10-02 | Information Disclosure | POS UI | mitigate | Remove cost from interface; no reference to cost in component code |
| T-02-10-03 | Elevation of Privilege | API contract | mitigate | API contract (select block) is single source of truth for what data is exposed; enforced at database query level |
</threat_model>

<verification>
1. Run `npm run dev` to start dev server
2. Navigate to /login, log in as CASHIER
3. Open browser DevTools → Network tab
4. Go to /cashier/pos, type "test" in search to trigger product search API call
5. Check the /api/cashier/products request/response:
   - Response should contain: id, name, sku, sellingPrice, storeQty, warehouseQty
   - Response should NOT contain: cost field
6. Confirm TypeScript compiler has no errors: `npx tsc --noEmit`
</verification>

<success_criteria>
- GET /api/cashier/products no longer returns cost field in response
- Product interface in POS page does not include cost
- CASHIER role cannot see unit cost or margin data via API
- SUPERADMIN and FINANCE endpoints continue to expose cost (not modified in this plan)
- No TypeScript errors after changes
</success_criteria>

<output>
After completion, create `.planning/phases/02-operations/02-10-SUMMARY.md`
</output>
