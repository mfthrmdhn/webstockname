---
phase: 02-operations
plan: 08
type: execute
wave: 7
gap_closure: true
depends_on: []
files_modified:
  - app/admin/products/page.tsx
autonomous: true
requirements: [INV-01, INV-02, PROD-02]

must_haves:
  truths:
    - "Product form has input fields for store_qty and warehouse_qty"
    - "Form validates quantities are positive integers"
    - "Form submission includes store_qty and warehouse_qty in POST payload"
    - "Quantities are stored in database and visible in product list"
  artifacts:
    - path: "app/admin/products/page.tsx"
      provides: "Product creation/edit form with store_qty and warehouse_qty inputs"
    - path: "app/api/admin/products/route.ts"
      provides: "API endpoint accepting and persisting store_qty and warehouse_qty"
  key_links:
    - from: "app/admin/products/page.tsx"
      to: "app/api/admin/products/route.ts"
      via: "form POST with store_qty, warehouse_qty"
      pattern: "fetch.*api/admin/products"
---

<objective>
Add missing stock quantity input fields to the product creation form.

Purpose: Superadmin cannot specify initial inventory when adding products; all products created with 0 stock.
Output: Product form with store_qty and warehouse_qty inputs, form submission validates and persists quantities.
</objective>

<execution_context>
@/Users/miftahramadhan/Projects/webstockname/.claude/get-shit-done/workflows/execute-plan.md
@/Users/miftahramadhan/Projects/webstockname/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/miftahramadhan/Projects/webstockname/.planning/PROJECT.md
@/Users/miftahramadhan/Projects/webstockname/.planning/ROADMAP.md
@/Users/miftahramadhan/Projects/webstockname/.planning/STATE.md
@/Users/miftahramadhan/Projects/webstockname/.planning/phases/02-operations/02-CONTEXT.md

<!-- From 02-CONTEXT.md D-05: store_qty and warehouse_qty must be added to Product model -->
<!-- From 02-CONTEXT.md: These fields default to 0 if not provided -->
<!-- From 02-VERIFICATION.md: gap states "can add product but cannot input store or warehouse stock quantity" -->
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add store_qty and warehouse_qty input fields to product form</name>
  <files>app/admin/products/page.tsx</files>
  <action>
Locate the product creation form in app/admin/products/page.tsx. Add two new input fields:

1. **Field: Store Quantity (store_qty)**
   - Input type: number
   - Min: 0
   - Default: 0
   - Label: "Store Quantity"
   - Required: false (defaults to 0)

2. **Field: Warehouse Quantity (warehouse_qty)**
   - Input type: number
   - Min: 0
   - Default: 0
   - Label: "Warehouse Quantity"
   - Required: false (defaults to 0)

Position these fields after the cost field in the form.

Update the form submission handler to include store_qty and warehouse_qty in the POST payload:

```typescript
const payload = {
  name: ...,
  sku: ...,
  sellingPrice: ...,
  cost: ...,
  store_qty: parseInt(formData.store_qty) || 0,
  warehouse_qty: parseInt(formData.warehouse_qty) || 0
}
```

Add client-side validation: if store_qty or warehouse_qty are provided, they must be >= 0. Show error message if invalid.
  </action>
  <verify>
    <automated>grep -n "store_qty\|warehouse_qty" /Users/miftahramadhan/Projects/webstockname/app/admin/products/page.tsx | wc -l</automated>
  </verify>
  <done>
- Product form includes two new numeric input fields: store_qty and warehouse_qty
- Both fields accept numeric input with min=0
- Form submission includes store_qty and warehouse_qty in POST payload
- Client-side validation prevents negative values
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire form fields to API endpoint and validate persistence</name>
  <files>app/api/admin/products/route.ts, app/admin/products/page.tsx</files>
  <action>
**Step A — Verify API endpoint accepts store_qty and warehouse_qty**

Check app/api/admin/products/route.ts POST handler:
- Extract store_qty and warehouse_qty from request body
- Pass them to prisma.product.create() (or .update() if editing)
- Validate quantities are integers >= 0, return 400 if invalid

If the API endpoint does not yet accept these fields, add them:

```typescript
const { name, sku, sellingPrice, cost, store_qty, warehouse_qty } = await req.json()

// Validate
if (store_qty < 0 || warehouse_qty < 0) {
  return res.status(400).json({ error: 'Quantities must be >= 0' })
}

// Create
await prisma.product.create({
  data: {
    name,
    sku,
    sellingPrice,
    cost,
    storeQty: parseInt(store_qty) || 0,
    warehouseQty: parseInt(warehouse_qty) || 0
  }
})
```

**Step B — Test form submission**

1. Navigate to /admin/products
2. Fill form: name, sku, selling price, cost, store_qty=10, warehouse_qty=5
3. Submit form
4. Verify product appears in list with correct quantities
5. Refresh page — quantities persist (were saved to database)
6. Try submitting with negative store_qty — form should show error

**Step C — Verify quantities in product list**

If the product list table exists on the same page or in /admin/products, verify it displays store_qty and warehouse_qty columns so superadmin can see what was just entered.
  </action>
  <verify>
    <automated>curl -X POST http://localhost:3000/api/admin/products -H "Authorization: Bearer <token>" -H "Content-Type: application/json" -d '{"name":"Test","sku":"TST-001","sellingPrice":100,"cost":50,"store_qty":10,"warehouse_qty":5}' | grep -q "store_qty" && echo "API accepts store_qty" || echo "API missing store_qty"</automated>
  </verify>
  <done>
- API endpoint accepts store_qty and warehouse_qty in POST body
- API validates quantities are non-negative integers
- Products created with specified quantities
- Product list displays quantities
- Quantities persist across page refresh
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→server | Form submits quantities; must be validated server-side |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-08-01 | Tampering | Product quantities | mitigate | Validate quantities >= 0 on server before persisting; database CHECK constraint if possible |
| T-02-08-02 | Information Disclosure | API response | mitigate | Only SUPERADMIN can access product creation API (enforced via rbacMiddleware) |
</threat_model>

<verification>
1. Navigate to /admin/products — form loads
2. Fill product form with valid data including store_qty=5, warehouse_qty=10 — submit succeeds
3. Product appears in list with store_qty=5 and warehouse_qty=10 visible
4. Refresh page — product still shows correct quantities
5. Try submitting with store_qty=-1 — form shows validation error before submission
6. Try submitting with store_qty="abc" — form treats as 0 or shows validation error
</verification>

<success_criteria>
- Product form displays two new input fields for store_qty and warehouse_qty
- Both fields accept numeric input with minimum 0
- Form includes validation preventing negative values
- POST /api/admin/products accepts store_qty and warehouse_qty in payload
- API validates quantities are non-negative integers (returns 400 if invalid)
- Products created with specified quantities persisted to database
- Product list displays quantities for verification
</success_criteria>

<output>
After completion, create `.planning/phases/02-operations/02-08-SUMMARY.md`
</output>
