---
phase: 02-operations
plan: 07
type: execute
wave: 7
depends_on: []
files_modified:
  - app/login/page.tsx
  - app/api/auth/login/route.ts
  - lib/auth/client.ts
autonomous: true
requirements: [AUTH-01, AUTH-02]

must_haves:
  truths:
    - "Login page loads without errors"
    - "Users can submit login credentials (email/password)"
    - "Valid credentials return JWT and refresh token"
    - "Users are redirected to role-appropriate dashboard on login"
    - "Invalid credentials show error message"
  artifacts:
    - path: "app/login/page.tsx"
      provides: "Login form component with email/password inputs"
    - path: "app/api/auth/login/route.ts"
      provides: "POST endpoint accepting email/password, returning JWT"
    - path: "lib/auth/client.ts"
      provides: "Client-side login() function"
  key_links:
    - from: "app/login/page.tsx"
      to: "app/api/auth/login/route.ts"
      via: "fetch POST with credentials"
      pattern: "fetch.*api/auth/login"
    - from: "app/api/auth/login/route.ts"
      to: "prisma.user"
      via: "findUnique by email, verify password, return token"
      pattern: "prisma.user.findUnique"
---

<objective>
Diagnose and fix the critical login failure blocking all users from authenticating.

Purpose: Login is broken, preventing any staff from accessing the system.
Output: Functional login page and auth API endpoint, tested across all three roles (CASHIER, FINANCE, SUPERADMIN).
</objective>

<execution_context>
@/Users/miftahramadhan/Projects/webstockname/.claude/get-shit-done/workflows/execute-plan.md
@/Users/miftahramadhan/Projects/webstockname/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/miftahramadhan/Projects/webstockname/.planning/PROJECT.md
@/Users/miftahramadhan/Projects/webstockname/.planning/ROADMAP.md
@/Users/miftahramadhan/Projects/webstockname/.planning/STATE.md
@/Users/miftahramadhan/Projects/webstockname/.planning/phases/02-operations/02-CONTEXT.md

<!-- From VERIFICATION.md: login was reported as broken in Test 2 -->
<!-- No specific error details logged; need to investigate the auth flow end-to-end -->
</context>

<tasks>

<task type="auto">
  <name>Task 1: Investigate login failure root cause</name>
  <files>app/login/page.tsx, app/api/auth/login/route.ts, lib/auth/client.ts</files>
  <action>
Diagnose why login is not working by checking the following:

1. **Check app/login/page.tsx** — Verify the login form exists and has email + password inputs, onSubmit handler that calls a login function.

2. **Check app/api/auth/login/route.ts** — Verify POST endpoint exists:
   - Accepts email and password in request body
   - Queries prisma.user by email
   - Compares submitted password against bcrypt hash in database
   - Returns JWT accessToken and refreshToken on success
   - Returns 401 with error message on failure

3. **Check lib/auth/client.ts** — Verify login() function exists:
   - Calls POST /api/auth/login with email/password
   - Stores JWT in localStorage (or httpOnly cookie per Phase 1 decisions)
   - Returns success or error

4. **Identify the breakage** — Look for:
   - Missing fields in request/response (e.g., route expects 'password' but form sends 'pwd')
   - API route not returning JWT (returning only user object, or undefined)
   - Form submit handler not calling login() function
   - CORS or middleware blocking the request
   - Missing error handling preventing user from seeing what went wrong

5. **Document findings** — Record what is broken and where

Do NOT fix yet; diagnosis first.
  </action>
  <verify>
    <automated>grep -rn "app/api/auth/login" /Users/miftahramadhan/Projects/webstockname/app/api/auth/login/route.ts</automated>
  </verify>
  <done>Root cause identified: [login flow issue diagnosed, e.g., "API endpoint missing", "form not calling submit", "JWT not being returned", etc.]</done>
</task>

<task type="auto">
  <name>Task 2: Implement login fix and verify it works for all roles</name>
  <files>app/login/page.tsx, app/api/auth/login/route.ts, lib/auth/client.ts</files>
  <action>
Based on the root cause identified in Task 1, implement the fix:

**Common fixes to check:**
- If form is missing inputs: add email and password fields with proper name attributes
- If form not calling API: add onSubmit handler that calls login(email, password)
- If API not querying database: add prisma.user.findUnique({ where: { email } })
- If password not being verified: add bcryptjs.compare(submittedPassword, hash)
- If JWT not being returned: ensure response includes { accessToken, refreshToken }
- If client not storing token: ensure lib/auth/client.ts stores JWT in localStorage
- If redirect not happening: ensure successful login redirects to /admin (superadmin) or /finance (finance) or /cashier/pos (cashier)

**Test the fix:**
1. Start the dev server: `npm run dev`
2. Navigate to http://localhost:3000/login
3. Log in with a test user from each role:
   - SUPERADMIN user (email/password from seed or created in Phase 1)
   - FINANCE user
   - CASHIER user
4. Verify:
   - Form submits without error
   - JWT is returned from API
   - Token is stored in localStorage
   - User is redirected to appropriate dashboard
   - Subsequent page refreshes maintain the session

If any step fails, debug the specific issue (e.g., "email not found in database" → verify seed data, or "password hash mismatch" → verify bcrypt hashing is consistent).
  </action>
  <verify>
    <automated>curl -X POST http://localhost:3000/api/auth/login -H "Content-Type: application/json" -d '{"email":"test@example.com","password":"password123"}' | grep -q "accessToken" && echo "API returns JWT" || echo "API missing JWT"</automated>
  </verify>
  <done>
- Login page loads without errors
- Form accepts email and password input
- POST /api/auth/login endpoint returns JWT on valid credentials
- Invalid credentials return 401 error
- Successful login redirects to role-appropriate dashboard (admin/finance/cashier)
- All three roles (SUPERADMIN, FINANCE, CASHIER) can log in
- Token persists across page refresh
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→server | Login form sends plaintext password over HTTPS to API |
| server→database | API queries user by email; compares submitted password against bcrypt hash |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-07-01 | Spoofing | Login endpoint | mitigate | Password verification via bcryptjs; email uniqueness enforced in schema |
| T-02-07-02 | Tampering | JWT tokens | mitigate | JWT signed with HS256 + secret key; stored in httpOnly cookie if possible, localStorage if not |
| T-02-07-03 | Repudiation | Auth flow | mitigate | Login event logged to AuditLog (implemented Phase 1); identifies user attempting login |
| T-02-07-04 | Information Disclosure | Password handling | mitigate | Passwords hashed with bcryptjs before storage; no plaintext in logs or responses |
| T-02-07-05 | Denial of Service | Login rate limiting | accept | Not implemented Phase 2; add brute-force protection Phase 3 if needed |
| T-02-07-06 | Elevation of Privilege | RBAC at login | mitigate | JWT includes user.role; middleware enforces role-based access at every protected endpoint |
</threat_model>

<verification>
1. Navigate to /login — page loads without console errors
2. Submit login form with valid superadmin credentials — JWT is returned and stored
3. Navigate to /admin — page loads (not redirected to login) — proves token is valid
4. Submit login form with invalid credentials — error message displays
5. Log in as FINANCE, navigate to /cashier/pos via URL — redirected to /login (middleware blocks access)
6. Log in as CASHIER, check token in localStorage — contains user.role = "CASHIER"
</verification>

<success_criteria>
- Login page functional and visually renders
- Form submits email/password to /api/auth/login
- API endpoint validates credentials against bcrypt hash
- API returns JWT on success, 401 error on failure
- Client stores JWT and redirects to role dashboard
- All three roles can authenticate
- Role-based access control is enforced post-login
</success_criteria>

<output>
After completion, create `.planning/phases/02-operations/02-07-SUMMARY.md`
</output>
